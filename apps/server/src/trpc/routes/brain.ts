import { router } from '../trpc';

export const brainRouter = router({
  // Brain functionality removed for Gmail-only version
  // This router is kept for API compatibility but all endpoints are disabled
});
